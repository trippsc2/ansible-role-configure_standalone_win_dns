---
galaxy_info:
  author: Jim Tarpley
  namespace: trippsc2
  role_name: configure_standalone_dns_to_testing_ad_domain
  description: This role configures DNS on a standalone (non-domain) Windows machine to use the domain controller for DNS and registers its IP address in DNS.
  license: MIT

  min_ansible_version: '2.11'

  platforms:
  - name: Windows
    versions:
      - '2019'
      - '2022'

collections:
  - community.windows

dependencies: []
